// Temlate Literals

// function printdata(sname, age) {
//     let toPrint = "Student Name is " + sname + " and age is " + age;
//     console.log(toPrint);
// }

// let sname = " manish";
// let age = 22;

// printdata(sname, age);


function printdata(sname, age) {
    let toPrint = `Student Name is  ${sname}   and age is   ${age} `;
    console.log(toPrint);
}

let sname = " manish";
let age = 22;

printdata(sname, age);




